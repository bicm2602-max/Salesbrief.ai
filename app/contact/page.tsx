export const metadata = { title: "Contact | SalesBrief AI" };
export default function ContactPage() { const email = process.env.NEXT_PUBLIC_SUPPORT_EMAIL || (process.env.NODE_ENV === "development" ? "support@example.com" : ""); return <main className="mx-auto max-w-3xl space-y-4 px-6 py-20 text-slate-300"><h1 className="text-3xl font-semibold text-white">Contact</h1><p>For support, account, privacy, or deletion requests, contact <strong>{email || "a support contact will be published before launch"}</strong>.</p>{!email ? <p className="text-sm text-slate-500">Founder action required: set NEXT_PUBLIC_SUPPORT_EMAIL before launch.</p> : null}</main>; }
